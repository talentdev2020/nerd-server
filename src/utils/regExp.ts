/**
 * A sequence of digits, at least one.
 * A valid number could include
 * at most one decimal point.
 * if the point is in the string, then
 * decimals would be before and after it.
 */
export const validNumberRegExp = /^\d+(\.\d+)?$/;
